# Reading Lamp — 英語多読アプリ

Extensive Reading (ER, 多読) の研究知見にもとづいて設計した PWA です。スマホのホーム画面に追加してネイティブアプリのように使えます。

## 設計の土台となっている多読の原則

このアプリの仕様は、以下の原則から逆算して作られています。機能を足したり変えたりするときは、この表と照らして判断してください。

| 原則 | アプリでの実装 |
|---|---|
| テキストは「やさしい」こと（既知語95〜98%、未知語は100語あたり1〜2語） | レベル1〜10のグレーデッド・リーダー準拠の語彙帯。生成プロンプトの最優先ルールとして明記 |
| 「少し易しすぎる」レベルから始め、退屈になってから上げる | 読後に3択で読みごこちを申告。難のときは即座に1段階下げ、同じレベルで「易」が3回連続したときだけ1段階上げる |
| 量が学習効果の中核（年間100万語が目標） | ホーム画面の主役は累計語数カウンタ。1万→100万語の節目バーを表示 |
| 読書速度・流暢さの向上が主要な成果指標 | 本文が実際に表示されていた時間だけを計測し、直近5回の有効な語数÷読書時間から語/分 (wpm) を表示 |
| 辞書使用は最小限。分からない語は飛ばす | 本文画面に辞書機能を一切置かない。ホームで毎回リマインド |
| 読書そのものが報酬。読後テストは行わない | 振り返り・語彙リスト・理解度クイズの機能は持たない |
| 読み手が読みたいものを自分で選ぶ | テーマ選択（7テーマ＋自由入力＋おまかせ） |
| 退屈な本はやめてよい（脱落の自由） | 本文画面に「これはやめて、別の文章にする」。語数は加算せず、記録には残る |
| 毎日同じ時間に少量でも継続 | 連続日数と1日の目標語数（既定1,500語）を表示 |

## レベルの目安

| Lv | 語彙帯 | 目安 |
|---|---|---|
| 1 | 300語 | ごく易しい。短く単純な文 |
| 2 | 600語 | 日常的な話題 |
| 3 | 800語 | 単純な物語が読める |
| 4 | 1,000語 | 時制が自在に |
| 5 | 1,400語 | 描写や説明が増える（初期値） |
| 6 | 1,800語 | 抽象的な話題も少し |
| 7 | 2,500語 | 複文や比喩が自然に |
| 8 | 3,000語 | 論説的な文章も |
| 9 | 4,000語 | 原書に近い語彙 |
| 10 | 無制限 | 簡略化なし。一般書・報道と同水準 |

初期値はレベル5です。TOEIC 750 / B2 相当の力があっても、多読では実力より低いところから始めるのが正解です。「退屈なほど楽」と感じるまで上げないでください。上げるのを急ぐと、多読ではなく精読になってしまいます。

### WPM計測とレベル調整

- WPMは本文画面が表示され、ページが前面にある時間だけで計算します。別タブへの移動、画面ロック、アプリのバックグラウンド滞在は読書時間に含めません。
- 10秒未満の読了、または10未満・600超のWPMは誤操作や計測異常の可能性が高いため、WPM統計から除外します。ホームのWPMは、直近5回の有効記録について合計語数を合計読書時間で割って算出するため、短い文章だけが平均を大きく動かしません。語数と読了記録は通常どおり残ります。
- 「難しかった」は次の文章から1レベル下げます。「ちょうどよかった」は現在レベルを維持します。
- 「やさしすぎた」は同じレベルで3回連続した場合だけ1レベル上げます。途中でやめる、「ちょうどよかった」を選ぶ、または設定からレベルを手動変更すると連続回数はリセットされます。

## オフラインの文章バンク（APIキー不要）

初回からオフライン文章バンクが標準で有効です。Anthropic APIを一切呼ばずに、あらかじめ用意した文章から出題します。通信費・API利用料がかからず、一度アプリ一式を読み込んだ後は完全オフラインでも使えます。

- 現在収録：**1,000篇**（ID `s001`〜`s1000`、レベル1〜10 × 全7テーマ：Fantasy/stories・Famous books・Nature and animals・World affairs・Everyday life・History・Science）
- 文章バンクは文体品質監査済み。旧生成文に偏っていた定型副詞とタイトル形式を整理し、ID・レベル・テーマは維持しています。
- 各テーマ・各レベルに複数篇を収録済み。今後も随時追加予定
- `Famous books` は、十分古い古典作品の筋・人物・設定をもとに、多読用として新規に書き下ろしたリテリングです。原文や現代の翻訳文は転載していません。
- 選んだテーマとレベルの組み合わせの中で、一度読んだ文章は他のストックがなくなるまで再表示されません。読み尽くすと記録をリセットして次の周からまた出題します（直前に読んだ1篇だけは連続を避けます）
- レベル調整（読後の3択フィードバック）は文章バンクモードでも動作します

### 1,000篇の語彙レベル監査

全篇について、一般英語での語の使用頻度、極低頻度語、8文字以上の語、平均文長を監査しました。詳細は`VOCABULARY_AUDIT.md`、篇ごとの数値は`vocabulary_audit_1000.csv`に収録しています。

- 運用ガードレール内：474篇
- 構文のみ要確認：140篇
- 語彙要確認：261篇
- 優先確認（語彙指標を2種類以上超過）：125篇

要確認は自動的な不合格ではありません。固有名詞や題材固有の科学語などを編集者が確認するための抽出です。監査による本文の自動書き換えやレベルの自動変更は行っていません。

文章を追加する場合は、`stories.json`に同じ形式（`id`・`level`・`topic`・`title`・`text`・`wordCount`）でオブジェクトを追加すると反映されます。本文を変更した場合は、`wordCount`も本文から再計算してください。

## 中身

```
index.html      画面構成
styles.css      見た目
app.js          ロジック本体（レベル管理・生成・計測・記録）
manifest.json   PWA設定
sw.js           オフライン用 service worker
stories.json    オフライン文章バンク（1,000篇）
icons/          アプリアイコン
VOCABULARY_AUDIT.md        1,000篇の語彙監査サマリー
vocabulary_audit_1000.csv  篇ごとの監査指標と判定
```

## 必要なもの

- HTTPS ホスティング先（PWAとしてインストールするには `http://localhost` か `https://` が必須）
- AI生成モードも使う場合のみ、Anthropic の API キー（<https://console.anthropic.com/settings/keys>。従量課金）

## 公開方法（おすすめ：GitHub Pages、無料）

1. GitHub に新しいリポジトリを作り、このフォルダの中身一式をアップロード
2. Settings → Pages で公開元を「main ブランチ / root」に設定
3. 発行された `https://アカウント名.github.io/リポジトリ名/` にスマホでアクセス
4. iPhone Safari なら共有 →「ホーム画面に追加」、Android Chrome なら右上メニュー →「アプリをインストール」

Netlify・Vercel・Cloudflare Pages へのドラッグ＆ドロップでも構いません。

## 使い方

1. テーマを選んで「文章バンクから読みはじめる」
2. 必要に応じて右上の歯車からレベルと1日の目標語数を調整
3. **辞書を引かずに**最後まで読む。分からない語は飛ばす
4. 「読み終えた」→ 読みごこちを3択で申告（これがレベル調整に使われます）
5. 語数・読む速さ・累計が記録される

新しいアプリ版が準備できると、画面下部に更新通知が表示されます。「更新する」を押すと新しいキャッシュへ切り替えて自動的に再読み込みします。「あとで」を選んだ場合は、次回起動時またはアプリへ戻ったときに再確認します。

ホームの「読書履歴の分析」を開くと、読了篇数、直近7日の語数、1篇平均語数、途中終了率、WPMの変化、テーマ別・レベル別の傾向を確認できます。途中終了の理由も集計されます。

AI生成を使う場合は、設定で「オフライン文章バンク」をオフにし、Anthropic APIキーを入力します。「AI生成する文章の目安語数」はこのモードでだけ表示・使用されます。オフライン文章バンクでは、各篇に保存された適切な長さをそのまま使います。また、自由テーマの指定はAI生成モード専用です。

しんどいと感じたら、途中で「これはやめて、別の文章にする」を押してください。理由を「難しすぎた」「興味を持てなかった」「長すぎた・時間不足」「用事などで中断」「その他」から選べます。「難しすぎた」のときだけ次から1レベル下げ、ほかの理由ではレベルを変えません。語数は加算されず、途中でやめたこと自体にペナルティはありません。

## 記録の書き出し

設定画面の「記録を書き出す (JSON)」で全履歴をダウンロードできます。日付・テーマ・語数・wpm・有効読書秒数（`activeSeconds`）・計測有効性（`wpmValid`）・読書前後のレベル・読みごこち申告・途中終了理由が入っています。

「記録を復元する (JSON)」では、書き出したファイルを読み込み、現在の記録を残したまま重複を除いて追加します。日付、語数、WPM、レベルなどを検証してから保存し、不正なファイルでは現在の記録を変更しません。端末内には新しい順で最大2,000件を保持します。

## アクセシビリティ

- 設定画面と途中終了理由画面に、読み上げ用のダイアログ名・説明・モーダル属性を設定
- 開いたときにダイアログ内へフォーカスを移し、Tabキーをダイアログ内で循環
- Escapeキー、背景クリック、閉じるボタンで終了し、元の操作位置へフォーカスを戻す
- 背景を操作対象外にし、モーダル表示中の本文計測を一時停止
- スライダーの現在値を読み上げ用テキストとして提供
- 復元結果とエラーをライブ領域で通知

## 注意点

- 標準のオフライン文章バンクではAPIキーを使用しません。
- AI生成モードのキーは`localStorage`や`sessionStorage`へ保存せず、開いているページのメモリ内だけに保持します。ページの再読み込み・終了時に消え、旧版が`localStorage`へ保存したキーも起動時に自動削除します。
- AI生成時はブラウザからAnthropic APIへ直接キーを送ります。通信先はCSPでAnthropicと同一オリジンのみに制限し、外部フォントなどの第三者通信も削除しています。ただし、ページを開いている間は同じページ内のJavaScriptがキーを利用できるため、信頼できる場所に配置したアプリだけを使ってください。
- 多人数へ公開する場合は、利用者にAPIキーを入力させず、認証・利用上限を備えたサーバー側プロキシへ移行するのが最も安全です。本版はGitHub Pagesなどの静的公開を維持するため、メモリ内保持方式を採用しています。
- API 利用には従量課金が発生します（1セッション数円程度が目安）。
- 生成される文章は毎回AIが新規に書き下ろすオリジナルです（実在の書籍・記事からの引用ではありません）。

## カスタマイズ

- レベルと語彙帯 → `app.js` の `LEVELS`
- 節目の語数 → `app.js` の `MILESTONES`
- テーマ一覧 → `index.html` の `topicSelect` と `app.js` の `TOPIC_POOL`
- 生成の指示内容 → `app.js` の `buildSystemPrompt()`
- 配色 → `styles.css` 冒頭の `:root`
