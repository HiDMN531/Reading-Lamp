# Reading Lamp 公開・実機確認チェックリスト

対象版: 2.10.6

## 1. 自動検証

```bash
npm install
npm test
npx playwright install chromium
npm run test:ui
```

`npm test`は文章・メタデータ・版番号・初心者向け設定に加え、秋の20篇が各レベルに2篇ずつ入っていることを確認します。`npm run test:ui`はスマホ幅の初心者向け読書フローと保存済みレベルの維持を実ブラウザで確認します。

## 2. Android Chrome

- オンラインで初回起動し、レベル1が選ばれ、最初の候補が短い1篇だけ表示される
- 最初の読了では「英語だけで1篇読めました」と表示され、レベル1の最初の5篇はWPMを読了画面に表示しない
- レベル1の読了が10篇に達した後は、新しい短文を含む3候補が表示される
- レベル1で「難しすぎた」を選ぶと、レベルを維持して短い別の文章が出る
- 既存利用者の保存済みレベルと1日の目標が変わらず、通常の候補が3篇表示される
- 初回案内で「英語を、英語のまま読む練習」と、日本語訳・単語帳・理解度テストを置かない理由が読める
- ホームの「詳しい使い方」と設定の「Reading Lampの使い方」から同じ使い方画面を開ける
- 初めて本文を開いたときだけ、辞書を使わず読む案内が表示され、閉じた後は再表示されない
- 「難しすぎた」で途中終了すると、レベル調整と辞書の代わりにレベルを下げる案内が表示される
- 獲得済みの灯りカラーを変更すると、約1.6秒かけてアプリ全体の灯りが変化する
- 端末で「視差効果を減らす」を有効にした場合、灯りカラーの変化はアニメーションしない
- 最初の一篇を読了すると、読了画面に次回の案内が一度だけ表示される
- 「今週のまとめ」は初期状態で閉じており、開くと読了篇数が正しく見える
- 週・月の記録、読後画面、リワード名の日本語が短く自然で、表示値と食い違わない
- 最後の読了を14日以上前にしたテスト記録では、短めの候補3篇と再開表示が出る
- Pixelのホーム画面へ追加し、起動時の円形アイコン外周に角や突起が残らない
- ホーム画面の円形・角丸・スクワークル表示で、ランプと本が切れない
- インストール後にオフラインで起動できる
- 読書中にホームへ戻り、再起動後にスクロール位置から復元できる
- 画面ロック中の時間がWPMへ加算されない
- 更新通知の「更新する」で再読み込みされる
- 旧版で英文を端末に保存し、新版へ更新後に通信を切っても読書を開始できる
- 通信を戻すと新版の英文を保存でき、再度オフラインにしても読める
- JSONバックアップを端末へ保存し、ファイル確認前には「最終バックアップ」が変わらない
- バックアップを復元して履歴・既読判定・お気に入り・リワードが戻る
- 同じ組み合わせで未読が残り1〜2篇でも、既読が候補に混ざらない
- 設定とリワード画面をTalkBackで操作できる

## 3. iPhone Safari

- 「ホーム画面に追加」後、Reading Lampの名前とアイコンが表示される
- セーフエリアとステータスバーに表示が重ならない
- オフライン再起動、読みかけ復元、PWA更新が動く
- 英文を保存した状態で更新した後、機内モードにしても英文を開ける
- 更新版の英文保存に失敗しても、旧版の英文から読み始められる
- 設定画面でキーボードを開いても保存ボタンへ移動できる
- JSONバックアップを「ファイル」へ保存し、実際にJSONを確認した後に保存確認を押せる。共有ボタンが出る場合は共有先からファイルへ保存する
- 保存したJSONを復元し、既読の文章が新規候補として表示されない
- 文字サイズ26px、行間2.2でも本文と操作ボタンが欠けない
- VoiceOverでダイアログ名、進捗、閉じる操作が読み上げられる

## 4. 公開前の運営設定

- 配布ページに運営者名または屋号を記載する
- 実際に受信できる問い合わせ先を記載する
- `config.json`の`supportEmail`へ問い合わせ用メールを設定する
- 別のアドレスから`Reading.Lamp012@gmail.com`へ送信し、受信と迷惑メールフォルダを確認する
- iPhoneの問題報告画面で「メールで送る」を開き、宛先・文章ID・本文を確認して手動送信する
- 「端末に保存」はメールへ送られないことを確認する
- 自動収集する場合だけ`analyticsEndpoint`と`storyReportEndpoint`を設定する
- 送信APIに本文サイズ制限、レート制限、JSON検証、重複防止を設定する
- `privacy.html`と`terms.html`を実際の運営内容に合わせて確認する
- `config.json`変更後にService Workerのキャッシュ名を更新する

## 5. 公開後の定期確認

- 文章問題報告を週1回確認する
- 途中終了率が高い文章を月1回確認する
- 主要フローを更新のたびに再実行する
- AndroidとiPhoneの実機確認を大きなUI変更ごとに行う
- 事実を含む文章は確認日と参照先を定期的に見直す
